services:
  evm-height-checker:
    build:
      context: .
    container_name: evm-height-checker
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
