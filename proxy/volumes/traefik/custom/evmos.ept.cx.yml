http:
  routers:
    evmos_ept_cx:
      entryPoints:
        - web
        - websecure
      service: evmos_ept_cx
      rule: Host(`evmos.ept.cx`)
      tls:
        certResolver: letsencrypt
  services:
    evmos_ept_cx:
      loadBalancer:
        servers:
          - url: "h2c://lava-provider-m-1:22002"
        passHostHeader: true
